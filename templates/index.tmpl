<div class="container">

    <div class="jumbotron">
        <h1>{{.Title}}</h1>
        <p>{{.Description}}</p>
        <p class="text-right"><a class="btn btn-primary btn-lg">日程をえらブーン</a></p>
    </div>

</div>
